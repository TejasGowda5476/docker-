pipeline{
    agent any
    stages{
        stage("for pull"){
            steps{
                sh "docker run -d --name dev1 httpd"
            }
        }
        stage("for awscli"){
            steps{
                sh"aws.html"
            }
        }
    }
}
