pipeline{
    agent any
    stages{
        stage("for pull"){
            steps{
                sh "docker run -d --name dev2 httpd"
            }
        }
        stage("for awscli"){
            steps{
                sh"/var/lib/jenkins/workspace/${JOB_Name}/aws.yml"
            }
        }
    }
}
